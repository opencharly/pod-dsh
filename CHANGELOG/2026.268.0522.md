# 2026.268.0522 — chore(schema): migrate the pod-dsh charly.yml stamp to 2026.261.1747

## Summary

Migrates the `pod-dsh` `charly.yml` stamp from `2026.249.2125` to the current schema HEAD `2026.261.1747`. The `check-dsh-pod` bed was relocated into this repo (PR #4), so this repo's project must be at schema 261 for a schema-261 charly to resolve it.

**The 2-file diff:**
- `charly.yml` `version: 2026.249.2125 → 2026.261.1747` (produced by `charly migrate`; no other content change);
- `.github/workflows/deploy.yml` — the CI charly clone pin `v2026.256.1316 → v2026.266.1654`, because the old pin's compiled spec ceiling (max `2026.249.2125`) cannot load the migrated 261 stamp (`config schema 2026.261.1747 is newer than this charly supports (max 2026.249.2125)`; the `candy` check failed). `v2026.266.1654` carries the schema-261 loader — the same bump `layer-charly#7` needed.

Content commits `c5ab3ad` (stamp) + `daffa4d` (CI pin).

## How tested

**R10 — the changed project RESOLVES at 261, proven by the A/B resolution on the real bed** (the same command that failed before):

```
# BEFORE — the pre-change stamp (249):
$ charly check run check-dsh-pod
charly: error: command "check": InvokeProvider build:project op=resolve: build project: …/pod-dsh/charly.yml:
  schema 2026.261.1747 is required (found "2026.249.2125"). Run: charly migrate
$ echo $?
1

# AFTER — the changed stamp (261):
$ charly check run check-dsh-pod
charly check run check-dsh-pod: [image-build] PASS after 1m36.833s [allowlisted=1 cache=139/150]
charly check run check-dsh-pod: [check-image] START (charly check box; …)
```

The project moves from **load-failure (schema rejected)** to **resolving and building the `dsh-app` image** — the exact behavior the stamp exists to enable. This is a real, failable before/after on the touched project.

**The workflow half's gate is the `candy` CI check**, which builds charly at the pinned tag and runs `charly box validate`:

```
# at the OLD pin v2026.256.1316 (max schema 2026.249.2125):
  build  The candy manifest validates at the pinned charly
  charly: error: command "validate": validation error: …/pod-dsh/charly.yml: config schema 2026.261.1747
    is newer than this charly supports (max 2026.249.2125). Update charly …
  ##[error]Process completed with exit code 1.

# at the NEW pin v2026.266.1654 (schema 261): the check-runs on head 316dc7427
  build | completed/success
```

**The migration is the tool's own output:**

```
$ charly migrate
migrated to schema 2026.261.1747
$ grep -m1 '^version:' charly.yml
version: 2026.261.1747
$ git diff --stat
 charly.yml | 2 +-
```

**On the bed's FULL run:** the complete `check-dsh-pod` green run is the `plugin-dsh` consumer PR's gate (the compiled-in `command:dsh` plugin this project composes comes from `plugin-dsh`; this repo hosts the bed). This PR's own change is the stamp, and its proof is the A/B resolution above.

## Project-rulebook rule-compliance

- **R0** — `/charly-build:migrate`, `/charly-internals:git-workflow` loaded first.
- **R1** — the failure was root-caused: a 249 stamp in a repo whose bed a 261 charly must resolve; the A/B run shows the mechanism.
- **R2** — the whole change is the stamp; the resolution is proven in-tree (not deferred — only the FULL bed run is the consumer's, since the plugin is the consumer's).
- **R3** — one schema source (`charly.yml`'s `version:`); the `charly migrate` verb is the single owner.
- **R4** — no workaround; the sanctioned `charly migrate` produced the change.
- **R5** — no legacy path; the 249 stamp is replaced.
- **R6** — no force-push; two content commits (the stamp + the CI pin) plus body-refresh no-ops, squashed on landing.
- **R7** — the changed project fails to load before the change (pasted `exit=1`) and resolves + builds after (pasted), on a `disposable: true` bed.
- **R8** — the emitted schema stamp is the migrate tool's output.
- **R9** — the migrate ran on the worktree; the resolution ran on the worktree binary.
- **R10** — the project RESOLVES at 261 and builds the bed image (the load-bearing change proven live); the complete bed's green run is the `plugin-dsh` consumer PR's gate.
- **Disposable-only autonomy** — the `check-dsh-pod` bed is `disposable: true`.

## Change classification

Config (schema-stamp migration) + CI workflow (the charly clone pin). Verification gate: `charly check run check-dsh-pod` — A/B: fails to load at 249, resolves + builds at 261 — for the config half; the workflow half's gate is the `candy` CI check (builds charly at the new pin + `charly box validate`), which the pinned-256 pin failed and the `v2026.266.1654` pin passes (`.github/workflows/deploy.yml:25`). Attribution tier: `analysed on a live system` (the changed path ran live with retained output; the complete bed green run is the consumer's).

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*